# Fotos del armario

1. Coloca imágenes en `input/<categoria>/` (camisa, blusa, pantalon, etc.).
2. Opcional: `pip install rembg` para fondos transparentes.
3. Corre `npm run process-closet`.
4. Importa las PNG desde la app (Armario → + Prenda) o empaqueta un backup JSON.
